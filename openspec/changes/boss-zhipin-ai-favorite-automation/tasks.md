## 1. 设置模型与持久化基础

- [x] 1.1 在 `src/types` 新增设置类型定义（基础设置、高级设置、默认值与校验结果）
- [x] 1.2 新增 `settingsService`，封装 `localStorage` 的读取、写入、默认值回填与异常处理
- [x] 1.3 为设置服务补充单元级验证（默认值加载、非法数据回退、保存失败处理）

## 2. 设置界面与入口改造

- [x] 2.1 在主界面右上角新增设置入口（Element Plus 按钮/图标按钮）
- [x] 2.2 新增设置面板组件，分为“基础设置”和“高级设置”两个分区
- [x] 2.3 将设置面板与 `settingsService` 打通，实现加载、编辑、保存与反馈提示

## 3. 主界面内容重构（Prompt 驱动）

- [x] 3.1 移除 popup 中现有非预期展示内容，仅保留任务执行相关区域
- [x] 3.2 在内容区域新增 prompt 输入框与执行按钮
- [x] 3.3 增加站点检测结果、执行状态与错误信息展示区域

## 4. 页面 I/O 与自动化编排实现

- [x] 4.1 在 `chromeMcpService` 增加候选人列表读取、候选人详情读取、收藏按钮触发等能力封装
- [x] 4.2 在 `usePageIoController`（或新增 orchestrator）实现顺序编排：站点校验→列表读取→逐个处理
- [x] 4.3 为编排流程增加可观测进度（当前候选人、成功数、失败数、失败原因摘要）

## 5. LLM 调用与收藏决策

- [x] 5.1 新增 `llmService`，封装 API 请求构建、响应解析与错误处理
- [x] 5.2 将“用户 prompt + 简历文本 + 候选人上下文”组装为模型输入并调用 LLM
- [x] 5.3 根据 LLM 结构化结果执行收藏或跳过，并记录判定理由

## 6. 随机等待与鲁棒性

- [x] 6.1 在每位候选人处理完成后插入 1-5 秒随机等待逻辑
- [x] 6.2 为单候选人处理增加超时与失败后继续策略，避免全流程中断
- [x] 6.3 为关键失败路径补充用户可读提示（站点不匹配、读取失败、LLM 失败）

## 7. 验证与交付

- [x] 7.1 运行类型检查与构建，修复新增改动导致的类型或构建错误
- [ ] 7.2 人工验证关键场景：站点检测、设置持久化、候选人流程、收藏动作、随机等待
- [x] 7.3 更新必要文档（README 或变更说明），记录配置项与使用前提
